<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.iut_orleans.as_2016_2017.besancon_pubert.gradle_cadavreexquis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity   android:name=".MainMenu"
                    android:label="MainMenu"
                    android:parentActivityName="LoginScreen"  >
        </activity>

        <activity   android:name=".NouvellePhrase"
                    android:label="NouvellePhrase"
                    android:parentActivityName="MainMenu">
        </activity>

        <activity   android:name=".Inscription"
                    android:label="Inscription"
                    android:parentActivityName="LoginScreen">
        </activity>

        <activity   android:name=".HistoriquePhrase"
                    android:label="HistoriquePhrase"
                    android:parentActivityName="MainMenu">
        </activity>

        <activity   android:name=".Evaluation"
                    android:label="Evaluation"
                    android:parentActivityName="HistoriquePhrase">
        </activity>


    </application>

</manifest>